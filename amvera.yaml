meta:
  version: 1.0

project:
  name: MiddleAgesEconomy
  language: python

deploy:
  type: service
  build:
    command: pip install -r requirements.txt
  run:
    command: python main.py
    ports:
      - 80